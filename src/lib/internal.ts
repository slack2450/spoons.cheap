import { Ranking } from '../types/Ranking';

export async function getRankings(): Promise<Ranking[]> {
  throw Error('TODO: Fix Rankings')
  /*const res = await axiosInstance.get('/v1/rankings');
  return res.data;*/
}
